# Backend - Smart College Management System

REST API with JWT auth + RBAC, MySQL (mysql2), and PDF export.

## Scripts
- `npm run dev` - runs `nodemon src/server.js`

## Environment variables
See `.env.example` in this folder.

## API base
All endpoints are under `/api/*`.

